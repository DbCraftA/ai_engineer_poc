# Results

This directory stores selected benchmark outputs, profiler traces and experiment observations when they are useful to keep.

Each result should include enough context to be interpreted later: hardware, device, dtype, PyTorch version, model, batch size and sequence length.

