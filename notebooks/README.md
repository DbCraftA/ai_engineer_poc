# Notebooks

Notebooks are the exploration layer of the lab.

They are useful for asking one focused question, manipulating tensors, visualizing results, and then moving stable knowledge back into `src/` and `tests/`.

They are not the source of truth. The source of truth is executable code plus pytest tests.

